services:
  - type: web
    name: gbcyber
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    autoDeploy: true
    envVars:
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: false
      - key: APP_KEY
        generateValue: true
      - key: APP_URL
        value: https://gbcyber.onrender.com
      - key: DB_CONNECTION
        value: pgsql
      - key: DB_HOST
        fromDatabase:
          name: gbcyber-db
          property: host
      - key: DB_PORT
        fromDatabase:
          name: gbcyber-db
          property: port
      - key: DB_DATABASE
        fromDatabase:
          name: gbcyber-db
          property: database
      - key: DB_USERNAME
        fromDatabase:
          name: gbcyber-db
          property: user
      - key: DB_PASSWORD
        fromDatabase:
          name: gbcyber-db
          property: password

databases:
  - name: gbcyber-db
    plan: free
